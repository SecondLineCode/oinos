<%= simple_form_for(@vineyard) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :vineyard_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
