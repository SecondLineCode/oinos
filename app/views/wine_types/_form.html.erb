<%= simple_form_for(@wine_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :api_id %>
    <%= f.input :wine_type %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
